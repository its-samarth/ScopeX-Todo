// styles.ts

import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
  text: {
    fontFamily: 'FiraCode-Regular', // Ensure this matches the font file name
  },
});

export default globalStyles;
